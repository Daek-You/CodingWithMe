#include <iostream>
using namespace std;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	long long N;
	cin >> N;

	cout << (N * (N - 1) * (N - 2)) / 6 << endl;
      cout << 3;
	return 0;
}